# Plan: Make Python Verification Green

## Goal
Get all Python unit tests passing in a clean environment with proper dependency installation.

## Current State Analysis
- 88 Python files in repository
- `/tests/python/` contains 9 test files
- Minimal `requirements.txt` with only 7 direct dependencies
- No pytest configuration or test infrastructure setup
- Tests use pytest (implied by test naming conventions)
- Tests likely depend on additional transitive dependencies not listed

## Required Steps

### 1. Environment Setup
- Use 4t (4tie) environment as specified by user
- Activate 4t virtual environment
- Install base requirements from `requirements.txt` in 4t env
- Install test dependencies (pytest, pytest-asyncio, etc.) in 4t env
- Install any missing transitive dependencies required by tests

### 2. Test Infrastructure Configuration
- Create basic pytest configuration if needed
- Set up proper test paths and import resolution for 4t environment
- Configure test environment variables for 4t project
- Verify 4t-specific import paths are correctly set up

### 3. Test Execution & Validation
- Run all tests in `/tests/python/` using 4t environment
- Identify and fix any failing tests
- Address import errors and dependency issues specific to 4t
- Ensure all tests pass in clean 4t environment

### 4. Verification
- Confirm all 9 test files pass in 4t env
- Validate results are reproducible across environments
- Document any fixes or changes required for 4t compatibility

## Expected Dependencies Missing From requirements.txt
Based on test file names and project structure, these are likely required:
- pytest
- pytest-asyncio
- anyio
- pytest-mock
- pytest-cov
- Additional FastAPI/Starlette test utilities
- Possibly trading framework dependencies (freqtrade?)

## Implementation Order
1. ✅ Install base dependencies
2. ✅ Install test dependencies
3. 🔍 Verify Python environment and paths
4. 🔍 Check for existing virtual environment
5. 🔍 Run tests to identify failures
6. Fix failures iteratively
7. Verify all tests pass
